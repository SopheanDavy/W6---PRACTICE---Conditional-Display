function Header(props) {
  return (
    <header id="header">
      <img src={props.logo} alt="PN Logo" />
      <h1>Students results for {props.batchName}</h1>
    </header>
  );
}

export default Header;






























    
